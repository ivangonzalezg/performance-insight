services:
  performance-insight:
    container_name: performance-insight
    build: .
    ports:
      - "7891:7891"
    volumes:
      - data:/usr/src/app/data
    environment:
      - NODE_ENV=development

volumes:
  data: